# Webcrawler_HTTP
webcrawler with some decent written test cases

Web Crawler is an automated tool designed to systematically browse websites and extract structured data. Starting from a given URL, it follows links within a specified depth and domain, collects relevant information such as page titles, URLs, and links, and stores the results in a structured format like JSON.
